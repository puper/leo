# QA 阶段工作流 (QA Phase Workflow)

## 目标

在研究-计划-执行流程后进行质量检查，确保改动正确。

## 检查范围

- 改动区域的代码正确性
- 控制流/数据流是否正确
- 是否符合项目约定

## 输出

QA 报告存放于 `.artifacts/qa/`，包含：
- 检查项清单
- 发现的问题
- 修复建议

## 原则

- **只读不写**: QA 阶段不修改源代码
- **聚焦改动区域**: 优先检查实际改动的部分
- **控制流/数据流**: 重点验证逻辑正确性
